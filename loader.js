const loader = document.querySelector('.loader');
const text = document.querySelector('.loader h1')

window.addEventListener('load', () => {

    loader.classList.add('fondu-out');
    text.classList.add('dispawn')

})